For use with TCPClient, to begin you must crate a settings.txt with the format<br>HOST:x.x.x.x<br>  PORT:xxx<br>
currently displays any messages sent by the client and sends any messages typed into the terminal back to client
If the client sends a file it will save it locally and add the line "this file is stored by the server to the end"
then send the file back to the client
